# Interactive Map — repository-ready package

This folder is intended to be copied **as-is** into the root of the existing repository.

Final structure:

```
/interactive-map/
  index.html
  interactive-map.html
  styles.css
  assets/
  categories/
  data/
  logo/
  tours/
```

Test URL after deployment:

`https://YOUR-DOMAIN/interactive-map/`

The package preserves the approved map functionality, categories, routes, geolocation, 5-second auto-tour, real images, tour pages, header, footer, and Wellness & Relax coordinates.

The header links back to the existing main site using `../index.html`.

For GitHub: use GitHub Desktop rather than the browser uploader because this package contains many files.
