V20 — TOUR PAGE HERO AND GALLERY IMAGE FIX

This package fixes broken hero backgrounds and gallery images on every tour page opened from the interactive map.

Changes:
- repaired missing local image paths in all tour HTML pages;
- assigned each page its matching tour hero image where original gallery files were unavailable;
- added a browser-side image fallback for dynamically generated gallery elements;
- retained V19 map card image corrections.

Install: copy the interactive-map folder over the existing interactive-map folder and replace files.
