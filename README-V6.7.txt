ATO V6.7 — HIDDEN GEMS DEFAULT SIDEBAR

CHANGE:
The compact Tour Finder preview on the home page no longer shows the top-ranked / obvious tours when no filtering is active.

DEFAULT STATE (no query + no filters):
Shows 2 lesser-known experiences from this curated Hidden Gems pool:
- Syedra Ancient City
- Gazipaşa Bays
- Horse Riding / Horse Farm
- E-Foil Experience & Training
- Stand Up Paddleboarding
- Anamur
- Private Fishing
- Green Canyon

The pair is stable during the day and rotates daily.

WHEN SEARCH OR ANY FILTER IS USED:
The compact preview immediately switches back to the actual filtered/relevant results.

The result count still shows the real total number of matching tours (e.g. 58).
No Hero, category, visual, or filtering logic was otherwise changed.

DEPLOY:
If V6.6 is already installed, replacing index.html is sufficient.
