ATO TOUR FINDER MASTER V3

INDEX
- index.html is self-contained: Finder CSS + JS + tour catalogue are embedded.
- This prevents stale Vercel/GitHub asset cache from showing an older Finder.

OTHER SITE PAGES
- Shared source files are included in assets/css, assets/js and assets/data.
- Use the same header Search Tours trigger id="headerTourSearch".
- When standardizing the whole site, load the shared Finder layer from the common header/layout and keep ONE catalogue source.

Version: MASTER-V3.1
